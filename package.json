{
  "name": "pulse",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "drift-action"
  ],
  "scripts": {
     "dev": "concurrently -n api,worker,rate-limiter,drift,web -c blue,green,yellow,magenta,cyan \"npm run dev -w apps/api\" \"npm run dev -w apps/worker\" \"npm run dev -w apps/rate-limiter\" \"npm run dev -w apps/drift-detector\" \"npm run dev -w apps/web\"",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "concurrently": "^10.0.1",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@clerk/nextjs": "^7.3.5"
  }
}
