{
  "name": "@pulse/node",
  "version": "0.1.0",
  "description": "Lightweight observability SDK for Node.js backends",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build",
    "publish:dry": "npm publish --dry-run",
    "publish:npm": "npm publish --access public"
  },
  "peerDependencies": {
    "express": ">=4.0.0",
    "fastify": ">=4.0.0"
  },
  "peerDependenciesMeta": {
    "express": { "optional": true },
    "fastify": { "optional": true }
  },
  "dependencies": {
    "fastify-plugin": "^4.5.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "express": "^4.18.2",
    "fastify": "^4.26.1",
    "typescript": "^5.5.0"
  }
}
