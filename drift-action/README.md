# Pulse Drift Check Action

Checks environment variable drift against a Pulse baseline and optionally fails the workflow when drift is detected.

Add the action to a workflow step with `project-key`, `environment`, and (optionally) `fail-on-drift` / `ignore-keys` inputs.

See the full guide at https://pulseobserve.com/docs/drift.
